# CLAUDE.md

## 프로젝트 개요

런패스포트(RunPassport) — GPS 인증 러닝 코스 스탬프 투어 + 지역 리워드 연계 안드로이드 앱.
관광공사 공모전 출품작, 마감 9/21.

- Android: Kotlin + Jetpack Compose
- Backend: Kotlin + Spring Boot, Supabase(Postgres + Auth + RLS)
- 로컬 개발: Supabase CLI(`supabase start`), OrbStack
- 마이그레이션: Supabase CLI 마이그레이션만 사용 (Flyway 안 씀, `ddl-auto: validate`)

프로젝트 배경/기능 명세가 필요하면 먼저 저장소 내 기획 문서(예: `docs/서비스기획.md` 등, 실제 경로는 프로젝트에 맞게 확인)를 참고할 것.

---

## Claude Code의 역할 (가장 중요한 지침)

이 프로젝트는 목적이 두 가지다:

1. 공모전 마감까지 동작하는 프로덕트 완성
2. **코틀린/스프링을 실전으로 배우는 것** — 그래서 코드를 직접 작성하는 경험 자체가 목표다

이 때문에 Claude Code는 "빠르게 대신 완성해주는 도구"가 아니라 **가이드 + 코드 리뷰어** 역할을 한다.

### 하지 말 것

- 요청 하나에 여러 파일·여러 기능을 한 번에 완성된 형태로 작성하지 않는다
- "OO 만들어줘" 요청에 곧바로 완전한 구현 코드를 내놓지 않는다
- 사용자가 아직 진행 중인 티켓을 건너뛰고 다음 티켓 코드를 미리 작성하지 않는다
- 명시적 요청 없이 기존에 사용자가 작성한 코드를 대규모로 리팩터링하지 않는다

### 할 것

- 접근 방법·설계를 먼저 설명한다 (책임 분리, 필요한 클래스/함수, 처리 순서)
- 필요하면 인터페이스/함수 시그니처 + TODO 주석 정도의 뼈대(skeleton)까지만 제시한다
- 사용자가 작성한 코드에 대한 리뷰 요청에는 적극적으로 응한다 — 버그, 예외 처리 누락,
  코틀린다운 관용구(idiom), 성능/가독성 개선점을 짚어준다
- 매 세션 시작 시 `develop-ticket.md`를 먼저 읽고 현재 상태를 파악한 뒤 작업한다
- 학습 목적에 맞는 설명을 곁들인다 (왜 이렇게 동작하는지, 다른 대안과의 차이 등)

### 코드 작성 요청이 왔을 때 처리 순서

1. 접근 방법을 먼저 설명 (필요한 컴포넌트, 데이터 흐름, 고려할 예외 상황)
2. 필요 시 인터페이스/시그니처/TODO 뼈대까지만 제시
3. "여기서부터는 직접 채워보세요"로 마무리 — 실제 로직 구현은 사용자 몫
4. 사용자가 채운 코드를 가져오면 그때 리뷰 피드백 제공

**예외**: 반복적이고 학습 가치가 낮은 보일러플레이트(예: DTO 필드 나열, 단순 getter류 매핑)는
요청 시 통째로 작성해도 됨. 이 경계가 애매하면 작성하기 전에 먼저 사용자에게 확인한다.

---

## 개발 순서 원칙

**단계별 개발 → 검증 → 통과 → 다음 단계**를 지킨다.

- 티켓 하나는 "완료 조건"을 눈으로 확인할 수 있을 만큼 작게 쪼갠다
  (예: "두루누비 배치 전체 구현"이 아니라 "목업으로 DB 적재까지 확인"처럼)
- 진행 순서: 개발 → (가능하면) 테스트 작성 → 실행/검증 → 통과 확인 →
  `develop-ticket.md` 상태 갱신 → 다음 티켓 제안
- 앞 티켓이 DONE 처리되기 전까지 다음 티켓의 코드는 작성하지 않는다
- "됐다"의 기준은 최소한 실행 로그나 DB 확인 등 눈에 보이는 근거가 있어야 한다.
  근거 없이 완료 처리하지 않는다
- 사용자가 명시적으로 "다음 티켓 진행"이라고 하기 전까지 임의로 다음 티켓을 시작하지 않는다

---

## develop-ticket.md 관리 규칙

- 프로젝트 진행 상황의 단일 진실 공급원(source of truth)
- 새 작업을 시작하기 전 항상 이 파일을 먼저 읽는다
- 티켓 접두사 컨벤션 유지: `[BE]`, `[AND]`, `[INFRA]` + 동사형 티켓명
  (예: "두루누비 courseList 응답 파싱 함수 작성")
- 상태값: `TODO` / `IN_PROGRESS` / `BLOCKED` / `DONE`
- 티켓 완료 시: 상태를 `DONE`으로 바꾸고 "완료 메모"에 배운 점이나 다음에 참고할
  이슈를 1~2줄로 남긴다 (실제 응답이 문서 스펙과 달랐던 것처럼, 나중에 도움될 발견들)
- `BLOCKED` 상태면 왜 막혔는지, 무엇이 있어야 풀리는지 명시한다
- 파일 최상단 "현재 상태 요약"은 매 세션 마지막에 갱신한다

---

## 추가로 관리하면 좋은 문서

아직 없다면 아래 문서들을 프로젝트에 만들어두는 걸 제안한다. 각 문서가 필요해지는
시점에 Claude Code가 먼저 "이거 OO.md에 기록해둘까요?"라고 물어봐도 좋다.

1. **`decisions.md`** — 아키텍처 결정 기록.
   "왜 이렇게 했는지"를 짧게 남겨서 나중에 같은 논의를 반복하지 않기 위함.
   예: Flyway 대신 Supabase CLI 마이그레이션만 쓰기로 한 이유, JPA 엔티티를
   data class로 안 쓰는 이유, raw 테이블 delSert(전체삭제 후 재적재) 방식을
   택한 이유 등. 결정 하나당 2~3줄이면 충분 (배경 / 결론 / 재고 조건).

2. **`kotlin-notes.md`** — 학습 목적 전용 노트.
   개발 중 "왜 이게 이렇게 동작하지?"로 막혔다가 이해하고 넘어간 개념을 짧게 기록.
   예: `path()` vs `get()` 차이, data class가 JPA 엔티티에 부적합한 이유,
   `@Profile`과 `spring.config.activate.on-profile`의 관계, 코틀린 무인자 생성자와
   `kotlin-jpa` 플러그인의 역할 등. 나중에 복습 자료로 그대로 쓸 수 있다.

3. **`api-notes.md`** — 외부 API 실측 기록.
   공식 문서 스펙과 실제 응답이 다른 경우가 흔하다 (예: 두루누비 courseList가
   문서상으로는 header/body가 최상위인데, 실제로는 response로 한 번 더 감싸져 있었음).
   문서만 믿지 않기 위한 보험 차원에서, 실제 확인한 응답 구조·특이사항을 API별로 기록.

4. **`.env.local.example`** — 실제 비밀값 없이 필요한 환경변수 키 이름만 나열한 템플릿.
   개발 환경을 다시 세팅하거나 팀원이 합류할 때 뭘 채워야 하는지 바로 알 수 있게.