spring:
  jpa:
    show-sql: false

logging:
  level:
    com.runpassport: INFO
    org.hibernate.SQL: WARN

jwt:
  secret: ${JWT_SECRET}
  access-token-validity-ms: 1800000     # 30분
  refresh-token-validity-ms: 2592000000 # 30일
